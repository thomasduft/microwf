toto:
(x) persisting workflow variables
(x) WorkflowItemQueue
(-) persist/resume work items
(x) Issue sample in client
(x) visualize workflow
  - see: https://github.com/mdaines/viz-examples/blob/master/script-tag/no-worker.html

done:
(x) rename IWorkflowDefinition.WorkflowType --> Name
(x) CreateViewModel needs an interface
  (x) default provider will read workflows from configuration
(x) workflow controller
  (x) list all workflows
  (x) start a workflow
  (x) list instances assigned to me
  (x) resume a workflow
(x) test 
  (x) TransitionContext
  (x) TriggerParam
  (x) TriggerResult
(x) add identity to WebApi
(x) add db seeder



EF Commands:
- see: https://docs.microsoft.com/en-us/ef/core/miscellaneous/cli/dotnet

dotnet ef migrations add <somename> -c DomainContext
dotnet ef database update -c DomainContext
dotnet ef database drop



NuGet Commands:
dotnet pack src/microwf.Core -c Release /p:PackageVersion=1.0.0 -o ./../../dist/nupkgs